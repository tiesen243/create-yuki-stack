{
  "name": "@{{ name }}/nextjs",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "{{ pkm }} with-env next build",
    "clean": "git clean -xdf .cache .next .turbo node_modules",
    "dev": "{{ pkm }} with-env next dev --turbopack",
    "format": "prettier --check . --ignore-path ../../.gitignore",
    "lint": "eslint",
    "start": "{{ pkm }} with-env next start",
    "typecheck": "tsc --noEmit",
    "with-env": "dotenv -e ../../.env --"
  },
  "prettier": "@{{ name }}/prettier-config",
  "dependencies": {
    "@{{ name }}/env": "workspace:*",
    "@{{ name }}/ui": "workspace:*",
    "next": "15.3.3",
    "react": "catalog:react",
    "react-dom": "catalog:react"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "catalog:tailwind",
    "@types/node": "22.15.30",
    "@types/react": "catalog:react",
    "@types/react-dom": "catalog:react",
    "@{{ name }}/eslint-config": "workspace:*",
    "@{{ name }}/prettier-config": "workspace:*",
    "@{{ name }}/tsconfig": "workspace:*",
    "dotenv-cli": "^8.0.0",
    "eslint": "catalog:tool",
    "prettier": "catalog:tool",
    "tailwindcss": "catalog:tailwind",
    "typescript": "catalog:tool"
  }
}
